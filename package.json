{
  "name": "morse-training",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Entrainement au code morse dans le navigateur, mobile et bureau.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.11",
    "vite-plugin-pwa": "^0.21.1"
  }
}
